development: &development
  # adapter: async
  adapter: redis
  url: redis://localhost:6379/
  channel_prefix: hanami_development

test:
  adapter: async

production:
  adapter: redis
  url: redis://localhost:6379/3
  channel_prefix: hanami_production
